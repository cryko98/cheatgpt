{
  "name": "cheatgpt",
  "version": "1.0.0",
  "private": true,
  "description": "CheatGPT — landing page + chat for the $CHEAT community.",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
